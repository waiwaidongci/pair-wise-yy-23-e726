name: braille-trainer
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-braille-trainer}-frontend
    env_file:
      - .env
    ports:
      - "${FRONTEND_PORT:-20111}:80"
