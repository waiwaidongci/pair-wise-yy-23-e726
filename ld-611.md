# ld-611 盲文点字学习训练器

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**盲文点字学习训练器（braille-trainer）**：纯前端盲文点字学习与练习工具，支持点阵字符卡片、听写练习、错题本和学习进度统计，数据存 IndexedDB。项目类型：**Web 前端应用**。

### 2. 核心数据模型（≥ 3 个，必须贯穿 store/API/组件/页面）

1. **点字字符（BrailleSymbol）**
   - 字段：id, cell_pattern, letter, pinyin, category, difficulty, audio_hint_key
   - 关键关系：被课程、练习题和错题本复用
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

2. **课程（Lesson）**
   - 字段：id, title, symbol_ids, stage, estimated_minutes, unlock_rule
   - 关键关系：组织点字字符学习路径
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

3. **练习会话（PracticeSession）**
   - 字段：id, lesson_id, mode, started_at, finished_at, score, mistake_count
   - 关键关系：生成多条 AnswerRecord
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

4. **答题记录（AnswerRecord）**
   - 字段：id, session_id, symbol_id, user_answer, correct, latency_ms, mistake_reason
   - 关键关系：驱动错题本和进度统计
   - 业务动作：创建、编辑、筛选、统计、导入导出
   - **此模型必须贯穿多模块，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，必须有路由配置）

1. **学习卡片** `/learn`
   - 消费实体/模型：BrailleSymbol + Lesson
   - 关键交互：点阵卡片、字符解释、按难度切换
   - 共享组件：BrailleCell, StatusBadge, LessonProgress

2. **练习模式** `/practice`
   - 消费实体/模型：PracticeSession + BrailleSymbol + AnswerRecord
   - 关键交互：识别点阵、听写模拟、即时反馈
   - 共享组件：BrailleCell, PracticePanel, ResultBadge

3. **错题本** `/mistakes`
   - 消费实体/模型：AnswerRecord + BrailleSymbol
   - 关键交互：按错误原因归类、重新练习、标记掌握
   - 共享组件：BrailleCell, ResultBadge, EmptyState

4. **学习进度** `/progress`
   - 消费实体/模型：Lesson + PracticeSession + AnswerRecord
   - 关键交互：进度趋势、正确率、难度分布
   - 共享组件：LessonProgress, ChartPanel, StatusBadge

### 4. 共享枚举/常量（≥ 2 组）

1. **PracticeMode**：CELL_TO_TEXT / TEXT_TO_CELL / LISTENING / MIXED
   - 常量位置：`frontend/src/constants/PracticeMode.ts` 或同名模块
   - 类型位置：`frontend/src/types/PracticeMode.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **SymbolCategory**：LETTER / NUMBER / PUNCTUATION / CONTRACTION
   - 常量位置：`frontend/src/constants/SymbolCategory.ts` 或同名模块
   - 类型位置：`frontend/src/types/SymbolCategory.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **MasteryLevel**：NEW / LEARNING / FAMILIAR / MASTERED
   - 常量位置：`frontend/src/constants/MasteryLevel.ts` 或同名模块
   - 类型位置：`frontend/src/types/MasteryLevel.ts` 或聚合类型文件
   - README 必须列出该枚举在 store、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 5. 共享前端组件清单（≥ 3 个）

- `BrailleCell`
- `LessonProgress`
- `PracticePanel`
- `ResultBadge`
- `ChartPanel`

### 6. 共享 hooks/utils 清单（≥ 2 个）

- `useBraillePattern`
- `usePracticeSession`
- `useIndexedDbStore`

### 7. 技术要求

- 英文短名（用于 Docker Compose）：`braille-trainer`
- 前端端口：`20111`
- 前端技术栈：React 18 + TypeScript + Vite + Material UI + Zustand + IndexedDB
- 状态管理必须使用独立 store，禁止全部写在组件内部 state
- 数据来源：localStorage / IndexedDB / 本地 mock 文件，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 即便是本地模拟，也要按模型分文件封装 async API
├── stores/               # Pinia/Zustand/Redux 独立 store，禁止全放组件 state
├── types/                # 数据模型类型定义
├── constants/            # 枚举、日志模板、错误信息、状态文案
├── constructors/         # 默认对象、表单对象、导入数据构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook/composable
├── pages/                # 至少 4 个路由页面
├── router/
├── utils/
├── mocks/
└── workers/              # 如有耗时计算，放 Web Worker
```

严禁合并职责到单一文件。


### 部署要求

本项目虽然是纯前端，也必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，只编排 `frontend` 服务，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=braille-trainer`、`FRONTEND_PORT=20111`
- 根目录 `.env.example`
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管
- `frontend/nginx.conf`：配置 `try_files $uri $uri/ /index.html;` 支持 SPA 路由
- 本地模拟数据放在 `frontend/src/mocks/`，禁止接入第三方 API

`docker-compose.yml` 必须满足：
- 顶层 `name: braille-trainer`
- `container_name: ${COMPOSE_PROJECT_NAME:-braille-trainer}-frontend`
- 前端端口映射：`${FRONTEND_PORT:-20111}:80`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后访问 `http://localhost:20111`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
